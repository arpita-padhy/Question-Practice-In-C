//wap to find the size of data types//
#include<stdio.h>
int main()
{
    int i;
    float f;
    char c;
    printf("size of %zu bytes \n",sizeof(i));
    printf("size of %zu bytes \n",sizeof(f));
    printf("size of %zu bytes \n",sizeof(c));
    return 0;
}
